import React from 'react';
import { useNavigate } from 'react-router-dom';
import { useLang } from '../lib/LanguageContext';
import { ArrowLeft } from 'lucide-react'; export default function ErrorPage({ code = 404 }: { code?: number }) { const { t } = useLang(); const navigate = useNavigate(); const getErrorContent = () => { switch (code) { case 401: return { title: 'Unauthorized', desc: 'Authentication required to access this node.' }; case 403: return { title: 'Forbidden', desc: 'You do not have clearance for this sector.' }; case 500: return { title: 'Internal Server Error', desc: 'The orchestrator encountered an anomaly.' }; case 503: return { title: 'Service Unavailable', desc: 'Upstream providers are currently degraded.' }; case 404: default: return { title: 'Not Found', desc: 'The requested vector does not exist in this space.' }; } }; const { title, desc } = getErrorContent(); return ( <div className="min-h-screen bg-primary flex flex-col items-center justify-center p-6 selection:bg-inverse selection:text-[#FAFAFA]"> <div className="max-w-md w-full text-center"> <h1 className="text-[120px] font-serif leading-none tracking-tighter text-[#121110] mb-4"> {code} </h1> <div className="h-px w-full bg-inverse/10 mb-8" /> <h2 className="text-xl font-medium text-[#121110] mb-2">{title}</h2> <p className="text-label text-muted mb-12 font-mono">{desc}</p> <button onClick={() => navigate('/')} className="inline-flex items-center gap-3 bg-white border border-[#121110]/10 px-6 py-3 text-micro font-mono uppercase tracking-[0.2em] text-[#121110] hover:border-[#121110]/30 transition-all duration-300 ease-out-expo active:scale-[0.98] shadow-sm hover:shadow-md rounded-none" > <ArrowLeft className="w-4 h-4" /> {t('Return to Base', '返回基地')} </button> </div> </div> );
}
